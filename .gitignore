/target
/target/**/*.rs.bk

# Secrets & runtime state (must never be committed)
rules.key
rules.pub
config.json
whitelist.json
scan_state.json
*.sig
ferroshield.log
*.pid
dashboard.crt
dashboard.key
dashboard.token

# Local data
.quarantine/
/feeds/*.txt
/feeds/

# Build artifacts
/dist/

# Chrome-generated cache when loading the unpacked extension
extension/_metadata/
